arr=[]
n=int(input("Enter the number: "))
l2=[]
for i in range(n):
    arr.append(int(input("Enter elements: ")))
print(arr)
#one method using slicing
# l2=arr[::-1]
# print(l2)
#another method using arr length
for i in range(len(arr)-1,-1,-1):
    l2.append(arr[i])
print(l2)
